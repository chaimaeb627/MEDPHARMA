<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion des Produits Médicaux{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: 'Nunito', sans-serif;
            background-color: #F4F6F8;
        }

        /* Sidebar */
        .sidebar {
            width: 260px;
            background: #343a40; /* Gris foncé pour un look plus moderne */
            color: #fff;
            padding: 40px 25px;
            position: fixed;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
        }

        .sidebar h5 {
            color: #28A745; /* Vert clair pour la marque */
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 40px;
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .sidebar a {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: #fff;
            font-size: 1rem;
            font-weight: bold;
            margin-bottom: 20px;
            padding: 10px 15px;
            border-radius: 8px;
            transition: background 0.3s ease, transform 0.2s ease;
        }

        .sidebar a:hover {
            background-color: #495057; /* Gris clair pour l'hover */
            transform: translateX(10px);
        }

        .sidebar a i {
            font-size: 1.5rem;
            margin-right: 15px;
        }

        .sidebar a span {
            font-weight: bold;
        }

        .sidebar .divider {
            margin: 30px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1); /* Gris clair pour la ligne */
        }

        /* Top Bar */
        .top-bar {
            position: fixed;
            top: 0;
            left: 260px;
            right: 0;
            background: #343a40; /* Gris foncé */
            color: #F5F5F5;
            padding: 18px 30px; /* Augmentation du padding pour élargir */
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            z-index: 999;
        }

        .top-bar .profile {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .top-bar .profile i {
            font-size: 1.8rem;
        }

        .top-bar .profile span {
            font-size: 1.2rem;
            font-weight: 600;
        }

        /* Main Content */
        .content {
            margin-top: 110px; /* Ajustement pour correspondre à la hauteur de la top-bar */
            margin-left: 260px;
            padding: 40px;
            background-color: #F4F6F8;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
                box-shadow: none;
            }

            .top-bar {
                left: 0;
            }

            .content {
                margin-left: 0;
            }
        }
    </style>
</head>

<body>
    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <h5>MEDPHARMA</h5>
        <a href="{{ path('admin_medicament_list') }}">
            <i class="bi bi-capsule"></i>
            <span>Produits Médicaux</span>
        </a>
        <a href="{{ path('admin_commande_list') }}">
            <i class="bi bi-cart-check"></i>
            <span>Commandes</span>
        </a>
        <div class="divider"></div>
    </div>

    <!-- Top Bar -->
    <div class="top-bar">
        <span class="title"></span>
        <div class="profile">
            <i class="bi bi-person-circle"></i>
            <span>Chaimae Benali</span>
        </div>
    </div>

    <!-- Main Content -->
    <div class="content" id="content">
        {% block body %}
        <h1>Bienvenue dans votre application</h1>
        <p>Découvrez l'ensemble des fonctionnalités et naviguez dans les menus pour gérer les différentes sections.</p>
        {% endblock %}
    </div>
</body>

</html>
